# Server Configuration
PORT=8080
GIN_MODE=debug
SES_FROM_EMAIL=noreply@manifimind.com

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=manifimind_crm
DB_SSL_MODE=disable

# JWT Configuration
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRATION_HOURS=24

# AWS Configuration (optional - leave empty for dev mode)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Password Encryption (REQUIRED for password vault)
# This master password is used to encrypt/decrypt passwords in pdat_passwd table
# WARNING: If you lose this password, all encrypted passwords will be unrecoverable!
# Use a strong, randomly generated password and store it securely
MASTER_PASSWORD=your-very-secure-master-password-change-this
