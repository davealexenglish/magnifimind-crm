version: '3.8'

services:
  database:
    build:
      context: ./db
      dockerfile: Dockerfile
    image: magnifimind-crm-database:latest
    container_name: magnifimind-db
    environment:
      POSTGRES_DB: magnifimind_crm
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - magnifimind-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 30s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: magnifimind-crm-backend:latest
    container_name: magnifimind-backend
    environment:
      PORT: 8080
      GIN_MODE: release
      DB_HOST: database
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_NAME: magnifimind_crm
      DB_SSL_MODE: disable
      JWT_SECRET: ${JWT_SECRET:-change-this-in-production}
      JWT_EXPIRATION_HOURS: 24
      MASTER_PASSWORD: ${MASTER_PASSWORD:-change-this-master-password}
      AWS_REGION: ${AWS_REGION:-us-east-1}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      SES_FROM_EMAIL: ${SES_FROM_EMAIL:-noreply@magnifimind.com}
    ports:
      - "8080:8080"
    depends_on:
      database:
        condition: service_healthy
    networks:
      - magnifimind-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: magnifimind-crm-frontend:latest
    container_name: magnifimind-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - magnifimind-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

networks:
  magnifimind-network:
    driver: bridge

volumes:
  postgres-data:
    driver: local
